<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="@drawable/card_background"
    android:layout_margin="4dp">

    <!-- 商品图片 -->
    <ImageView
        android:id="@+id/iv_shop_icon"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_launcher"
        android:layout_marginEnd="12dp" />

    <!-- 文字信息 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tv_shop_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary" />

        <TextView
            android:id="@+id/tv_shop_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="@color/text_secondary"
            android:maxLines="2"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/tv_shop_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#FF6B35"
            android:textStyle="bold" />

    <Button android:id="@+id/btn_wishlist" android:layout_width="32dp" android:layout_height="32dp" android:text="⭐" android:textSize="16sp" android:background="#E0E0E0" android:layout_marginStart="8dp" />
    </LinearLayout>

<Button android:id="@+id/btn_wishlist" android:layout_width="32dp" android:layout_height="32dp" android:text="⭐" android:textSize="16sp" android:background="#E0E0E0" android:layout_marginStart="8dp" />
    </LinearLayout>
